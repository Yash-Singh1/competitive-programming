// I don't want to write repeat code but the basic idea is that a graph is biconnected if it doesn't have any articulation points.

#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  // Get input...
  // Find articulation points...
  // Count connected components
  if (articulation_points == 0 && connected_components == 1) {
    cout << "yes\n";
  } else {
    cout << "no\n";
  }

  return 0;
}
